# React + Redux list of posts

This task is based on [Dynamic List of Posts](https://github.com/mate-academy/react_dynamic-list-of-posts#react_dynamic-list-of-posts). If you skipped that task in the React section, you need to complete it first and only then move on to connecting Redux to this task.

> Here is the [working DEMO](https://mate-academy.github.io/react_redux-list-of-posts/)

Learn the implemented App and the example and reimplement it with Redux having the next slice:
- `users` and remove `UsersContext`;
- `author` that is currently in the `App`;
- `posts` having 3 props `loaded`, `hasError` and `items`;
- `selectedPost`;
- `comments` with `loaded`, `hasError` and `items` props (keep the `visible` in the `PostDetails`);
- `NewCommentForm` should keep its state.

## Useful links
- [Why Redux Toolkit is How To Use Redux Today](https://redux.js.org/introduction/why-rtk-is-redux-today)
- [Learn Modern Redux Livestream](https://redux.js.org/introduction/getting-started#learn-modern-redux-livestream)

## Instructions
- Implement a solution following the [React task guideline](https://github.com/mate-academy/react_task-guideline#react-tasks-guideline).
- Use the [React TypeScript cheat sheet](https://mate-academy.github.io/fe-program/js/extra/react-typescript).
- This task does not have tests yet!
- Replace `<your_account>` with your Github username in the [DEMO LINK](https://NemH.github.io/react_redux-list-of-posts/) and add it to the PR description.

Це завдання базується на динамічному списку публікацій. Якщо ви пропустили це завдання в розділі React, вам потрібно спочатку виконати його, а потім переходити до підключення Redux до цього завдання.

Ось робоча ДЕМО-версія

Вивчіть реалізований додаток і приклад і перереалізуйте його, додавши в Redux наступний фрагмент:

користувачі та видаліть UsersContext;
автор, який зараз знаходиться в додатку;
публікації з 3 завантаженими пропсами, hasError та items;
selectedPost;
коментарі з пропсами loaded, hasError та items (збережіть видимість у PostDetails);
NewCommentForm має зберегти свій стан.
Корисні посилання
Чому Redux Toolkit – це те, як використовувати Redux сьогодні
Вивчіть сучасну пряму трансляцію Redux
Інструкції
Реалізуйте рішення, дотримуючись інструкцій щодо завдання React.
Використовуйте шпаргалку React TypeScript.
Це завдання ще не має тестів!
Замініть <your_account> на своє ім'я користувача Github у ПОСИЛАННІ ДЕМОНСТРАЦІЇ та додайте його до опису PR.
